From Idea to Profit with OnSpace

## Sitemap

- Generate or refresh `sitemap.xml` with all converter URLs:
  - `node scripts/generate-sitemap.mjs`
- The script reads `LENGTH_UNITS` from `index.html`, so newly added converter units are included automatically when regenerated.
